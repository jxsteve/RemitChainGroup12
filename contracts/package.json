{
  "name": "remitchain-contracts",
  "private": true,
  "version": "0.1.0",
  "description": "RemitChain smart contracts + Celo deploy tooling",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:alfajores": "hardhat run scripts/deploy.js --network alfajores",
    "deploy:celo": "hardhat run scripts/deploy.js --network celo",
    "keygen": "node scripts/keygen.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
